[pytest]
# Repo root on sys.path so tests import the `wp` package and the root-level
# modules (pentest_agent, http_client, ...) regardless of how pytest is invoked
# (`pytest`, `pytest tests/`, `pytest tests/test_sqli.py`, or from inside tests/).
pythonpath = .
